
set ui_allow_teamchange 1
if ( !getDstat( "cacLoadouts", "killstreak1" ) ) statwriteddl cacLoadouts killstreak1 193
if ( !getDstat( "cacLoadouts", "killstreak2" ) ) statwriteddl cacLoadouts killstreak2 194
if ( !getDstat( "cacLoadouts", "killstreak3" ) ) statwriteddl cacLoadouts killstreak3 203
if ( getDstat( "playerstatslist", "plevel" ) < 13 ) statwriteddl clantagstats selectedfeature 0
if ( getDstat( "playerstatslist", "rankxp" ) >= 2700 ) exec check_all_classes.cfg
exec reset_bindings.cfg

